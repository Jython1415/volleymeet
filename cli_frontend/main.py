# CLI; call functions from services.py to actually do the things

# Use a while loop so that the program doesn't stop running

# Interactive CLI, use numbers to navigate around the menus

# Design to interface with each of the backend routes

# Print hello world if main
if __name__ == "__main__":
    print("Hello World")