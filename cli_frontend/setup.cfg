[metadata]
name = cli_frontend
version = 0.1.0
description = CLI frontend for the meeting scheduling system

[options]
py_modules = main, services
install_requires =
    requests
    argparse

[options.packages.find]
where = src
